# 레퍼런스 기반 영상 연출·편집 최상위 작업 계약

상태: `authoritative`

원문:
`C:\Users\HP-5600G\.codex\attachments\779038a5-4191-4d82-9097-979a8e2ad8fb\pasted-text.txt`

## 0. 권위와 우선순위

이 문서를 영상 편집 하네스와 계정별 Tool의 최상위 창작 계약으로 적용한다.

우선순위는 다음과 같다.

1. 현재 작업에서 사용자가 명시한 요구·금지·범위
2. 이 최상위 작업 계약
3. `VIDEO_HARNESS_MASTER_BRIEF.md`
4. `ADAPTIVE_VIDEO_EDITING_HARNESS_ACTION_GUIDE.md`
5. 계정별 Skill, 계획서, Operator 카드와 자동 검사

하위 문서와 충돌하면 이 계약을 우선한다. 다만 저작권·개인정보·안전 규칙은 항상 지킨다.

## 1. 최종 목표

레퍼런스 영상의 컷 개수, 컷 위치, 재생시간을 표면적으로 복사하지 않는다.

실제 레퍼런스에서 다음 문법을 관찰하고 추출한 뒤 타깃 촬영본의 특성과 목적에 맞게 재구성한다.

- 촬영과 피사체 연출
- 인물·손·제품·오브젝트의 행동
- 한 컷 내부에서 발생하는 편집 사건
- 앞뒤 장면을 연결하는 시각적·감정적 인과
- 프레임 안의 사진·영상·그래픽·텍스트·시간 배치
- 카메라 움직임과 피사체 움직임의 관계
- 음악의 비트·온셋·프레이즈·강약과 화면 변화의 관계
- 감정 또는 광고 메시지의 상승과 결론
- 색·빛·질감·공간·스케일의 변화

편집을 다음 한 문장으로 정의한다.

> 촬영된 행동·오브젝트·빛·음악이 원인이 되어 한 컷 안에서 사진·영상·그래픽·텍스트·프레임·시간이 변화하고, 앞뒤 장면도 형태·움직임·감정의 인과로 연결되는 편집을 만든다.

촬영본에 필요한 재료가 없으면 디지털 줌, 프리즈, 반복 프레임, 무관한 효과로 위조하지 않는다. `적용 불가`를 판정하거나 다른 Operator, 단순한 폴백, 재촬영을 선택한다.

계정 이름은 완성 프리셋의 이름이 아니라 재사용 가능한 연출 Operator의 증거 출처로만 사용한다.

- Haku: 빛·계절·미세 행동·기억·감정적 이미지 연결
- Gawx: 손동작과 사진·영상·그림·그래픽의 화면 내부 상호작용
- Zamiri: 인물·패션·타이포그래피·음악의 팝 리듬
- Schiffer: 제품 물성·조명·카메라 움직임·임팩트

## 2. 편집 전 레퍼런스 분석

최종 편집 전에 실제 레퍼런스 전체를 직접 분석한다.

### 2.1 매크로 구조

- 시작
- 전개
- 상승
- 페이오프
- 마지막 잔상 또는 CTA

### 2.2 샷 단위 구조

- 샷 길이와 역할
- 구도와 카메라 높이
- 카메라 이동
- 피사체 움직임
- 행동 전·중·후의 변화

### 2.3 한 컷 내부 편집

- 사진·영상 삽입
- 분할 화면과 작은 창
- 마스크·트래킹·가림
- 시간차 레이어와 선택적 움직임
- 반복·루프·잔상
- 타이포그래피
- 물체와 그래픽의 상호작용
- 화면 비율·레이아웃 변화
- 그래픽이 없더라도 행동·빛·초점·표면·시간이 한 숏 안에서 의미 있게 변하는 사건

### 2.4 장면 연결 원리

- 형태·위치·방향 매치
- 행동의 원인과 결과
- 물성 연결
- 스케일 변화
- 감정적 대비와 회수

### 2.5 음악 상호작용

- 컷이 발생하는 비트와 프레이즈
- 소리가 이미지를 생성하거나 바꾸는 지점
- 음악이 멈출 때 화면이 비워지는지
- 드롭·악센트·프레이즈 변화가 화면 구조를 바꾸는지
- 음악이 배경인지 편집 사건의 원인인지

각 관찰에 다음을 기록한다.

- 레퍼런스 ID
- 정확한 타임코드
- 화면에서 실제로 보이는 현상
- 연출 목적과 원하는 시청자 반응
- 필요한 촬영본
- 관찰과 추론의 구분

모든 신규 proof와 final edit는 정확한 시간 구간별로 다음 10축을 모두 기록한다.

1. `narrative`: hook, setup, buildup, climax, resolution, CTA
2. `shot`: shot type, subject, framing, camera angle, camera motion, duration
3. `cut`: cut type, cut reason, incoming/outgoing motion, continuity
4. `timing`: pace, rhythm, beat position, pause, speed change
5. `transition`: type, duration, direction, visual anchor, sound anchor
6. `text`: content, font, position, emphasis, animation, duration
7. `color`: exposure, contrast, saturation, temperature, palette, mood
8. `audio`: dialogue, music, SFX, ambience, beat, volume curve
9. `motion`: zoom, pan, shake, tracking, easing, motion blur
10. `purpose`: attention, explanation, emotion, product emphasis, transition, CTA와 효과명·사용 시점·적용 장면·사용 이유·시청자 느낌

존재하지 않는 요소도 필드를 삭제하지 않고 `none`, `not_present`, `not_applicable`과 이유를 기록한다. 신규 proof는 이 10축 문법 파일과 검증 결과가 없으면 사용자 검토 단계로 보내지 않는다.

schema `1.1` proof와 final edit는 타임라인 전체 길이·FPS와 각 이벤트의 원본 clip ID·경로·SHA-256·source in/out·source/output frame range를 저장한다. 다중 원본 편집에서 하나의 `source_duration` 값이나 파일명만으로 provenance를 대신하지 않는다. 별도의 source manifest에는 권리, capture world, single-take, 기존 편집·숨은 retake 판정을 기록한다.

`감성적이다`, `힙하다`, `영화 같다` 같은 추상어만으로 분석을 끝내지 않는다.

### 2.6 영상 증거 수집 도구

Windows 작업공간에서 URL 또는 로컬 영상을 분석할 때는 가능한 경우 `tools\run-watch.cmd`를 첫 증거 수집기로 사용한다.

- 실행 전에 `owned`, `licensed`, `permissioned-analysis`, `public-reference-only` 중 하나의 권리 상태를 명시한다.
- 권리 상태가 `unknown`이면 다운로드·프레임 추출을 중단한다.
- 사용자가 음성 업로드를 명시적으로 허용하지 않으면 외부 Whisper 전사를 사용하지 않는다.
- 한글·공백 경로를 원문 그대로 전달하고 UTF-8 실행 경로를 유지한다.
- 추출된 모든 프레임을 직접 보고 타임스탬프 관찰을 기록한다.
- 샘플 프레임 추출은 전체 재생, 고밀도 컷 검토, 전환 전후 프레임 검토를 대신하지 않는다.
- 공개 레퍼런스 프레임과 미디어는 분석 전용이며 proof·final·promoted pack에 넣지 않는다.
- 추출 근거를 최종 10축 `timeline-grammar.json` 이벤트에 연결한다.

## 3. Operator 카드

각 연출·편집 기법을 다음 구조로 저장한다.

```yaml
operator_id:
display_name:
source_creator:
source_reels:
source_timecodes:

purpose:
visual_trigger:
within_shot_event:
shot_relationship:
music_relationship:

required_coverage:
  scale: [WS, MS, CU, ECU]
  action_phase: [before, during, after]
  elements: [hand, face, product, surface, space, negative_space]

compatible_situations:
incompatible_situations:

implementation:
  - Resolve Edit
  - Fusion
  - mask
  - tracking
  - Text+
  - compound_clip
  - retime
  - frame_layout

failure_modes:
simpler_fallback:
```

Operator는 효과 이름이 아니다. `목적`, `시각 트리거`, `한 컷 내부 변화`, `앞뒤 관계`, `음악 관계`, `필요 촬영본`, `실패 형태`를 모두 설명할 수 있어야 한다.

## 4. 타깃 촬영본 커버리지 감사

편집 전에 제공된 모든 촬영본을 직접 확인하고 다음을 기록한다.

- 사용 가능한 인물·제품·공간
- 실제 행동과 미세 표정
- 손·눈·표면·물성 디테일
- WS / MS / CU / ECU 존재 여부
- 움직임 시작과 종료의 핸들
- 그래픽·사진·영상창을 넣을 실제 여백과 표면
- 카메라와 피사체의 이동 방향
- 프레임레이트와 정당한 슬로모션 가능 여부
- 장면별 기술 품질
- 음악과 반응시킬 수 있는 시각적 사건

각 Operator를 다음 중 하나로 판정한다.

- `applicable`
- `conditional`
- `not_applicable`

촬영본이 부족하면 다음 순서로 처리한다.

1. 더 적합한 Operator를 선택한다.
2. 더 단순한 연출로 변경한다.
3. 필요한 재촬영 목록을 제시한다.

다음 방식으로 부족한 길이나 연출을 채우지 않는다.

- 마지막 프레임 복제
- 이유 없는 프리즈
- 자동 Ken Burns 확대·축소
- 갑작스러운 디지털 줌
- 반복 재생으로 길이 확보
- 무관한 플래시·글리치·전환 템플릿
- 검은 프레임 삽입

## 5. 연출안과 짧은 증명 영상

긴 완성본부터 만들지 않는다.

소재에 맞는 서로 다른 연출안 2~3개를 먼저 제시한다.

사용자가 특정 안을 이미 선택했거나 이 단계를 명시적으로 생략하라고 하지 않는 한, 한 가지 추천안만 제시하고 곧바로 편집하지 않는다. 원본 파일이 아직 없어도 현재 설명을 기준으로 `safe`, `recommended`, `experimental`의 차이와 필요한 촬영본을 먼저 밝힌다.

편집 시작 전 하드 게이트:

- treatment 출력에 `safe`, `recommended`, `experimental` 세 이름이 실제로 존재해야 한다.
- 세 안은 시각 전제, 화면 내부 사건, 음악 관계, 필요한 촬영본, 실패 위험 중 최소 두 축에서 서로 달라야 한다.
- 세 안이 없으면 레시피 작성·렌더·완성 선언을 중단한다.
- 사용자가 한 안을 명시적으로 선택한 뒤에만 선택된 안으로 수렴한다.

- 핵심 감정 또는 광고 기능
- 사용할 Operator 1~3개
- 필요한 촬영본
- 한 컷 내부 편집 사건
- 음악과 반응할 지점
- 마지막 이미지 또는 결과
- 예상 실패와 폴백

타깃 촬영본과 가장 잘 맞는 안을 고른 뒤 3~8초 `plain/proof`를 먼저 만든다.

### Plain

- proof와 같은 원본
- 오디오가 범위에 있으면 proof와 같은 음악 구간
- 기본 컷·최소한의 보정만 사용

### Proof

- plain과 같은 원본
- 오디오가 범위에 있으면 plain과 같은 음악 구간
- 검증하려는 Operator 1개, 최대 2개만 적용

오디오가 사용자의 현재 범위에서 보류되면 `deferred_by_user`로 기록하고 음악을 임의로 추가하지 않는다.

사용자는 오디오를 허용했지만 아직 트랙 권리 확인이 끝나지 않았다면 `deferred_by_rights`로 기록하고 무음으로 유지한다. 이 상태를 사용자 보류로 위장하거나 beat·sound anchor를 추정하지 않는다.

사용자가 외부 음악 사용을 허용하면 에이전트가 음악을 생성하지 않는다. 영상 동기화와 목표 게시 용도가 명시적으로 허용된 기존 공개 무료 트랙만 내려받고, 개별 트랙 페이지·공식 라이선스 URL·저작자·다운로드 시각·파일 해시·표기 의무·Content ID 위험을 저장한다. 권리가 불명확하거나 광고 목적과 NC가 충돌하거나 ND 조건이면 사용하지 않는다.

## 6. 화면 내부 편집

편집을 화면전환만으로 축소하지 않는다.

선택한 문법이 화면 내부 편집을 포함하면 proof 안에 최소 한 번의 명확한 사건을 넣는다.

- 손이 닿자 사진이 생성된다.
- 카메라 화면 안에서 다른 영상이 재생된다.
- 인물 주변에 기억 이미지가 행동이나 음악에 맞춰 축적된다.
- 작은 사진들이 하나의 결과 이미지로 정리된다.
- 근접 장면이 화면 내부 창으로 나타나 전체 행동을 설명한다.
- 한 화면 안에 서로 다른 시간이 동시에 존재한다.
- 오브젝트 움직임을 따라 텍스트·그래픽이 반응한다.
- 화면 일부만 움직이고 나머지는 유지된다.
- 빛·그림자·초점·표면·시간의 변화가 한 숏 안에서 감정 또는 정보를 바꾼다.

사건은 화면 속 행동·빛·오브젝트 또는 음악 사건에 의해 발생해야 한다.

다음은 실패다.

- 원인 없이 떠오르는 사진
- 맥락 없는 자막
- 장식용 분할 화면
- 무작위 글리치·플래시
- 저가형 템플릿처럼 보이는 전환
- 화면전환만 있는 내부 편집 proof

## 7. 음악 설계

오디오가 범위에 있으면 음악을 완성 후 배경으로 얹지 않는다. 러프 구조 전에 음악 지도를 만든다.

- BPM
- 강박과 약박
- 주요 온셋
- 프레이즈 시작과 종료
- 드롭과 브레이크
- 악기 추가·제거
- 감정 전환점

각 주요 화면 사건의 음악 관계를 기록한다.

- 킥: 이미지 생성
- 스네어: 패널 재배열
- 프레이즈 종료: 화면 비우기
- 드롭: 가장 넓은 공간 또는 최종 결과 공개
- 브레이크: 움직임 축소 또는 단일 이미지 유지

모든 컷을 비트에 맞추지 않는다. 중요한 화면 변화와 중요한 음악 사건만 의도적으로 연결한다.

음소거 상태에서도 연출이 이해되어야 하고, 소리를 켰을 때 힘이 증폭되어야 한다.

## 8. 평가 우선순위

다음 순서로 평가한다.

1. 목적과 감정이 명확한가
2. 타깃 촬영본의 강점을 살렸는가
3. 한 컷 안에 의미 있는 편집 사건이 있는가
4. 장면 사이에 시각적·감정적 인과가 있는가
5. 오디오가 범위에 있을 때 음악과 화면이 상호작용하는가
6. 레퍼런스 문법이 효과가 아니라 구조에 반영됐는가
7. 마지막 이미지가 결론이나 기억점으로 작동하는가
8. 기술적으로 자연스럽게 재생되는가

기술 오류 0개는 출발선이지 좋은 편집의 증거가 아니다. 정량값은 계기판이며, 연출의 인과와 감정이 운전대다.

다음은 기술적 `FAIL`이다.

- 의도하지 않은 정지
- 마지막 프레임 패딩
- 갑작스러운 줌 시작·리셋
- 프레임 중복으로 인한 잔떨림
- 검은 프레임
- 화면비 왜곡
- 기록되지 않은 숨은 색보정
- 저해상도 그래픽
- 트래킹 이탈
- 사진·그래픽 잘림
- 의도한 음악 사건과 화면 사건의 오프셋

## 9. 승인과 승격

AI는 다음을 스스로 선언하지 않는다.

- 스타일 완성
- Tool 완성
- 재현 성공
- 사용자 취향 학습 완료
- 대표 프리셋 승인

AI가 기록할 수 있는 상태:

- `draft`
- `technically_valid`
- `ready_for_user_review`

사용자 판정:

- `accepted`
- `partial`
- `rejected`

`accepted` 전에는 정식 프리셋으로 등록하지 않는다.

같은 Operator가 성격이 다른 소재·목적의 상황 3개 이상에서 각각 명시적 `accepted`를 받아야 재사용 가능한 기본 문법으로 승격한다.

승격 수치는 tested Operator별 `operator_id → accepted situation IDs`로 집계한다. 전역 승인 개수나 supporting Operator의 참여를 다른 Operator의 `1/3`, `2/3`, `3/3`으로 전용하지 않는다.

## 10. 사용자 제출 계약

가능한 범위에서 항상 다음을 함께 제출한다.

1. 사용한 레퍼런스 목록
2. 정확한 참고 타임코드
3. 관찰한 연출·편집 사건
4. 타깃 원본
5. plain 영상
6. proof 영상
7. 원본 / plain / proof 비교
8. 오디오가 범위에 있으면 음악 이벤트 지도
9. 적용한 Operator
10. 의도적으로 제외한 Operator
11. 촬영본 부족으로 구현하지 못한 부분
12. 정지·움찔·패딩·트래킹 검사
13. 사용자 판정 입력란

`잘 만들었다`, `스타일을 재현했다`고 먼저 주장하지 않는다. 사용자가 판단할 증거를 먼저 제시한다.

## 11. 사용자 교정 기록

피드백을 가능한 경우 다음 형식으로 기록한다.

```text
[타임코드]
00:02.40–00:03.10

[문제 종류]
화면 내부 편집 / 음악 / 장면 연결 / 움직임 / 그래픽 / 기술 오류

[현재 느껴지는 문제]

[원하는 인과]

[유지할 것]

[제거할 것]

[원하는 감정]

[판정]
accepted / partial / rejected
```

반복 피드백만 기본 규칙 후보로 올린다.

- 화면 내부 그래픽은 실제 행동이나 소리가 원인이 되어야 한다.
- 디지털 줌은 기본적으로 사용하지 않는다.
- 여운은 프레임 정지가 아니라 실제 미세 움직임으로 만든다.
- 음악은 컷 수를 결정하는 메트로놈이 아니라 화면 사건의 트리거로 사용한다.
- 촬영본에 없는 연출은 효과로 위조하지 않는다.
