# Visual critique

- Original source: directly reviewed as a complete 4 fps timeline.
- Plain and proof: directly reviewed as complete 8 fps timelines.
- All five segment boundaries: directly reviewed at 30 fps.
- Research verdict: **fail**.
- User verdict: pending.

## What works

The locked cup, table, window, and background are stable. The untouched tableau, pot arrival, stream onset, later empty table, and service hand are all real states from one licensed unedited take. No zoom, freeze, retime, reverse, crop animation, transition, or padding was used.

The pot arrival and stream onset boundaries are readable. The final cut removes only a long inactive hold before the service hand returns.

## Why this version fails

At the sustained-pour to release boundary, the red pot body abruptly disappears while the stream remains. The match on the stream is not enough to hide the missing physical hinge. The result has the same kind of lurch the user previously rejected.

The opaque floral cup also hides the liquid level, so this source cannot earn a full `motif-state-return` pass even if the action edit is repaired.

## Learned gate

A matched stream, limb, or motion direction does not justify skipping the object that owns the action. Preserve the physical release near-contiguously and compress only genuinely inactive time.
