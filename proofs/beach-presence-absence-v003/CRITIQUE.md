# Beach presence-to-absence v003 critique

## Research verdict

`pass`

## Direct comparison

- Both variants are 218 frames at 24 fps and use the same licensed unedited take.
- Plain gives 24 frames to the initial empty space and 38 frames to the final
  native water hold.
- Proof gives 16 frames to the initial empty space and 56 frames to the final
  native water hold.
- Proof therefore moves 18 frames of emphasis from setup and entry to the
  consequence without retiming, freezing, reversing, zooming, or padding.

## Visual findings

- The complete source contains empty environment → entrance → crossing → full
  frame exit → long moving empty environment.
- Boundary 02 now keeps leftward direction, subject scale, horizon, and a small
  adjacent displacement instead of the v002 right-to-center lurch.
- Boundary 03 is nearly spatially continuous.
- Boundary 04 completes the left exit and lets water and warm reflections remain
  visibly alive.
- The proof reaches the empty consequence earlier and holds it roughly 1.2
  seconds longer than plain, so absence reads as an authored destination.

## Limitation and routing rule

The source is 16:9 and the entrance/exit use both horizontal edges. A 9:16 cover
crop would destroy the event. Preserve the wide frame, use an explicit
non-cover layout, or request vertical coverage; never hide the mismatch with
animated reframing.

User verdict remains `pending`; promotion progress remains `0/3`.
