# Natural-language forward test — Park Density Wave v005

## Input

The request asks for a playful relationship to develop through action, for density to rise inside the frame, for a small live image to become the next full-frame action, and for an empty park path to remain after both people leave.

The input preserves the full natural-language request in `natural-language-request-v005.json`. Rights, existing-edit status, and material tags come from the directly reviewed unedited source and its `source-manifest.json`; the router does not infer them from keywords.

## Deterministic route

- Status: `ready_for_treatment_choice`
- Primary: `density-wave-release`
- Supporting evidence matches: `functional-memory-panels`, `presence-to-absence-hold`, `relationship-through-blocking`
- Primary material fit: `pass`, with no missing required coverage
- User verdict: `pending`
- Promotion increment: `0`

The three treatment levels remain separate:

- Safe: `density-wave-release` only.
- Recommended: add `functional-memory-panels` as one bounded in-frame density device.
- Experimental: retain all three supporting meanings for explicit testing, without treating them as independent user-approved Operators.

## Route → recipe → timeline proof

`validate_route_recipe_alignment.py` verifies that:

1. The routed primary Operator equals `recipe-v005.json`'s primary Operator.
2. Every recipe Operator is present in the route's evidence-backed candidate set.
3. The route, recipe, and timeline point to the same reviewed source manifest and audio policy.
4. Each of the three timeline events contains all ten axes.
5. Each event has source path, SHA-256, source frames, and output frames.
6. The route cannot infer user acceptance or increase promotion progress.

The canonical v005 artifacts pass. A regression mutation that changes the recipe primary to `experience-before-product` is rejected.

## Direct visual evidence already attached to this version

- The 13.88-second proof was directly reviewed in a dense 8fps full-sequence sheet.
- All six boundary and internal-event strips were directly reviewed.
- The v004-versus-v005 comparison shows that sequential support windows keep the central pair clearer than simultaneous windows.
- The 4.40-second inset-to-full-frame handoff uses the immediately following 25fps source frame.
- Both performers fully exit, leaving a real moving empty path from approximately 13.28 to 13.88 seconds.

The research verdict remains `partial_pass` because trustworthy captured full-speed playback is still unavailable and the aesthetic treatment needs the user's verdict.
