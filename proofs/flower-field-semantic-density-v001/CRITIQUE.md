# Flower-field semantic-density proof v001

- Source originals: seven complete same-session storyboards directly reviewed; five semantic roles selected and two divergent clips excluded.
- Reference comparison: complete dense timelines for `DWuZaCfE-WF` and `DQi8EW5k0-o` directly re-reviewed.
- Plain and proof: complete 8 fps timelines directly reviewed.
- All six proof boundaries: directly reviewed at 25 fps.
- Full-speed playback: not counted as reviewed.
- Research verdict: **pass**.
- User verdict: **accepted** (`density-wave-release = 1/3` distinct situations).

## What the proof establishes

The plain gives seven fragments equal duration and orders reaction, tactile detail and photography without a causal reading. The proof keeps the same 210-frame duration but changes the sequence into:

`flower-field premise -> pocket action preparation -> pocket action completion -> action owner -> readable camera act -> embodied arm-raise reaction -> native running release`

This creates the Haku-relevant density hinge for a functional reason. Early fragments accumulate place, texture and hand information quickly; the edit starts breathing only when the person and creative act become readable. The final two holds convert the catalogue into embodied feeling and forward movement.

The first render exposed a weak boundary where the reaction shot began before the person was visible. Direct 10 fps source review located the true action onset, and v002 moved the source-in from 5.0 to 5.5 seconds. The corrected 25 fps boundary now cuts from the camera action to already-emerging raised arms, then from the completed gesture to continuous running.

## Comparison with Haku evidence

`DWuZaCfE-WF` compresses sensory carriers, then gives human body, hand and environment longer recognition windows. `DQi8EW5k0-o` uses a more extreme fixed pulse, but likewise makes duration contrast—not zoom, shake or decorative transition—the structural device. This proof transfers that principle to coherent moving footage: every duration change introduces or clarifies a semantic role.

## Artifact and motion check

All cuts retain live native motion. No freeze, retime, reverse, animated crop, animated scale, synthetic zoom, overlay, text, last-frame padding or transition effect is used. The revised action boundaries show no stopped frame or synthetic camera jolt.

## Scope limits

This validates the editing grammar, not complete account imitation. The Pexels source palette is warmer, more saturated and more conventionally stock-like than Haku's restrained luminous imagery. The footage also lacks a second person, close facial emotion and a relational destination. Those require separate situations and user acceptance before general Operator promotion.

The user explicitly approved the comparison after review. This same-session flower-field situation now contributes **1/3 accepted situations to the tested `density-wave-release` Operator only**; two materially different situations are still required for that Operator's promotion. Supporting Operators receive no automatic increment.
